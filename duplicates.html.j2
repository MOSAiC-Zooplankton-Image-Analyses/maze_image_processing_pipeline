<!DOCTYPE html>
<html lang="en">

<head>
    <title>{{project_id}}</title>
    <style>
        body {
            background-color: black;
            color: white;
        }

        .row {
            display: flex;
        }

        .column {
            width: 100px;
        }

        .head {
            font-weight: bold;
            text-align: center;
            text-overflow: ellipsis;
            overflow: hidden;
            white-space: pre;
        }

        .content {
            width: 100px;
            height: 100px;
            /*border: 1px solid white;*/

            display: flex;
            justify-content: center;
            align-items: center;
        }

        .content img {
            object-fit: contain;
            margin: auto 0px;
            width: 100px;
            height: 100px;
        }
    </style>
</head>

<body>
    {% for row in rows %}
    <div class="row">
        <div class="column" style="border: 2px solid black;">{{row.frame_id}}</div>
        {% for item in row.objects %}
        {% if item is none %}
        <div class="column" style="border: 2px solid black;">
            <div class="content"></div>
        </div>
        {% else %}
        {# https://developer.mozilla.org/en-US/docs/Web/API/Clipboard/writeText #}
        <div class="column" style="border: 2px solid {{item.color}};">
            <div class="head">{{item.object_loc}}</div>
            <div class="content"><img loading="lazy" src="{{item.img_fn}}" /></div>
        </div>
        {% endif %}
        {% endfor %}
    </div>
    {% endfor %}
</body>

</html>